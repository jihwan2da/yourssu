spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:h2:tcp://localhost/~/desktop/study/jihwan2
    username: sa
    password:
    driver-class-name: org.h2.Driver